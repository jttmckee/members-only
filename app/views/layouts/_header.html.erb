<nav class = "navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class = 'navbar-toggle' data-toggle='collapse'
        data-target = '#myNavbar'>
        <span class="icon-bar"></span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
      </button>
      <%= link_to "Members Only", root_url, class:'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse" id='myNavbar'>
      <ul class='navbar navbar-nav'>
        <li><%= link_to 'Home', root_url %></li>
      </ul>
      <ul class = 'nav navbar-nav navbar-right'>
        <% if logged_in?(current_user) %>
        <li><%= link_to raw("<span class='glyphicon glyphicon-log-out'>
          </span> Logout"), logout_path, method:'delete' %></li>
        <% else %>
        <li><%= link_to raw("<span class='glyphicon glyphicon-log-in'>
          </span> Login"), login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>

</nav>
